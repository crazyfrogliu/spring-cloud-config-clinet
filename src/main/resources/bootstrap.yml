spring:
  application:
    name: config-client
  cloud:
    config:
#      uri: http://0.0.0.0:8888
      name: config-client
      profile: dev
      label: test
      discovery:
        enabled: true
        service-id: config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/

